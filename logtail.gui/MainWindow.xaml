<Window x:Class="logtail.gui.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:local="clr-namespace:logtail.gui"
mc:Ignorable="d"
Title="LogTail Viewer" Height="600" Width="1000"
Background="#1E1E1E"
Icon="Assets/Icons/default-128.ico"
AllowDrop="True"
DragEnter="Window_DragEnter"
DragOver="Window_DragOver"
DragLeave="Window_DragLeave"
Drop="Window_Drop">
    <Window.InputBindings>
        <KeyBinding Key="F1" Command="{Binding AboutCommand}"/>
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{Binding OpenFileCommand}"/>
        <KeyBinding Key="W" Modifiers="Ctrl" Command="{Binding CloseFileCommand}"/>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
        <KeyBinding Key="F" Modifiers="Ctrl" Command="{Binding FilterCommand}"/>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SettingsCommand}"/>
        <KeyBinding Key="R" Modifiers="Ctrl" Command="{Binding FileRotationSettingsCommand}"/>
        <KeyBinding Key="Space" Modifiers="Ctrl" Command="{Binding PauseResumeCommand}"/>
    </Window.InputBindings>
    <Window.Style>
        <Style TargetType="Window">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy}" Value="True">
                    <Setter Property="Cursor" Value="Wait"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>
    <Window.Resources>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="{x:Type MenuItem}" x:Key="SubMenuStyle">
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="Padding" Value="5,3"/>
        </Style>
    </Window.Resources>
    
    <DockPanel>
        <!-- Top Menu -->
        <Menu DockPanel.Dock="Top" Background="#2D2D30" Padding="0,5,0,5">
            <MenuItem Header="_File">
                <MenuItem.Resources>
                    <Style TargetType="Menu">
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="BorderThickness" Value="1"/>
                    </Style>
                </MenuItem.Resources>
                <MenuItem Header="_Open Log File..." Command="{Binding OpenFileCommand}" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Close" Command="{Binding CloseFileCommand}" InputGestureText="Ctrl+W"/>
                <MenuItem Header="Recent Files" ItemsSource="{Binding RecentFiles}">
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Setter Property="Header" Value="{Binding}"/>
                            <Setter Property="Command" Value="{Binding DataContext.OpenRecentFileCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            <Setter Property="CommandParameter" Value="{Binding}"/>
                            <Setter Property="ToolTip" Value="{Binding}"/>
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
                <Separator/>
                <MenuItem Header="_Exit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem.Resources>
                    <Style TargetType="Menu">
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="BorderThickness" Value="1"/>
                    </Style>
                </MenuItem.Resources>
                <MenuItem Header="_Filter..." Command="{Binding FilterCommand}" InputGestureText="Ctrl+F"/>
                <MenuItem Header="_Settings..." Command="{Binding SettingsCommand}" InputGestureText="Ctrl+S"/>
                <MenuItem Header="File _Rotation..." Command="{Binding FileRotationSettingsCommand}" InputGestureText="Ctrl+R"/>
                <Separator/>
                <MenuItem Header="{Binding PauseResumeButtonText}" Command="{Binding PauseResumeCommand}" InputGestureText="Ctrl+Space">
                </MenuItem>
                <MenuItem Header="_Refresh" Command="{Binding RefreshCommand}" InputGestureText="F5"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem.Resources>
                    <Style TargetType="Menu">
                        <Setter Property="BorderBrush" Value="White"/>
                        <Setter Property="BorderThickness" Value="1"/>
                    </Style>
                </MenuItem.Resources>
                <MenuItem Header="_About" Command="{Binding AboutCommand}" InputGestureText="F1"/>
            </MenuItem>
        </Menu>

        <!-- Bottom Status Bar -->
        <StatusBar DockPanel.Dock="Bottom" Background="{Binding StatusBarBackground}" Height="25">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" Foreground="White" Padding="5,0"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content - ListView with GridView -->
        <ListView x:Name="LogListView"
                  ItemsSource="{Binding LogEntries}" 
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseDoubleClick="LogListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Time">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp}" 
                                          Foreground="{Binding Foreground}"
                                          TextWrapping="NoWrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Level">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LevelText}" 
                                          Foreground="{Binding Foreground}"
                                          TextWrapping="NoWrap"
                                          FontWeight="Bold"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Source">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Source}" 
                                          Foreground="{Binding Foreground}"
                                          TextWrapping="NoWrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Message">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}" 
                                          Foreground="{Binding Foreground}"
                                          TextWrapping="NoWrap"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="BorderThickness" Value="0,1,0,1"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}">
                                    <GridViewRowPresenter Content="{TemplateBinding Content}"
                                                         Columns="{TemplateBinding GridView.ColumnCollection}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Navy"/>
                            <Setter Property="BorderBrush" Value="LightBlue"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Navy"/>
                            
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </DockPanel>
</Window>
